<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="theme-color" content="#050A3C">
    <title><%= title %> | Hidden Rabbit Panel</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Geologica:wght@500;600;700&family=Lilex:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preload" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@3.19.0/dist/fonts/tabler-icons.woff2" as="font" type="font/woff2" crossorigin>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@3.19.0/dist/tabler-icons.min.css">
</head>
<body>
    <!-- Mobile Menu Overlay -->
    <div class="mobile-overlay" id="mobileOverlay" onclick="toggleMobileMenu()"></div>
    
    <div class="app">
        <!-- Mobile Header -->
        <div class="mobile-header">
            <button class="mobile-menu-btn" onclick="toggleMobileMenu()" aria-label="Menu">
                <span></span>
                <span></span>
                <span></span>
            </button>
            <div class="mobile-logo">
                <span class="logo-icon">⚡</span>
                <span class="logo-text">Hidden Rabbit</span>
            </div>
            <div class="mobile-status">
                <span class="status-dot online"></span>
            </div>
        </div>
        
        <nav class="sidebar" id="sidebar">
            <div class="sidebar-head">
                <div class="logo">
                    <span class="logo-icon">⚡</span>
                    <div class="logo-content">
                        <span class="logo-text">Hidden Rabbit</span>
                        <span class="logo-sub">secure control surface</span>
                    </div>
                </div>
            </div>
            <ul class="nav-menu">
                <li><a href="/panel" class="<%= page === 'dashboard' ? 'active' : '' %>">
                    <span class="nav-icon"><i class="ti ti-smart-home"></i></span>
                    <span class="nav-label"><%= t('nav.dashboard') %></span>
                </a></li>
                <li><a href="/panel/stats" class="<%= page === 'stats' ? 'active' : '' %>">
                    <span class="nav-icon"><i class="ti ti-chart-line"></i></span>
                    <span class="nav-label"><%= t('nav.stats') %></span>
                </a></li>
                <li><a href="/panel/nodes" class="<%= page === 'nodes' ? 'active' : '' %>">
                    <span class="nav-icon"><i class="ti ti-router"></i></span>
                    <span class="nav-label"><%= t('nav.nodes') %></span>
                </a></li>
                <li><a href="/panel/groups" class="<%= page === 'groups' ? 'active' : '' %>">
                    <span class="nav-icon"><i class="ti ti-layout-grid"></i></span>
                    <span class="nav-label"><%= t('nav.groups') %></span>
                </a></li>
                <li><a href="/panel/users" class="<%= page === 'users' ? 'active' : '' %>">
                    <span class="nav-icon"><i class="ti ti-users-group"></i></span>
                    <span class="nav-label"><%= t('nav.users') %></span>
                </a></li>
                <li><a href="/panel/settings" class="<%= page === 'settings' ? 'active' : '' %>">
                    <span class="nav-icon"><i class="ti ti-settings-2"></i></span>
                    <span class="nav-label"><%= t('nav.settings') %></span>
                </a></li>
            </ul>
            <div class="sidebar-footer">
                <button
                    type="button"
                    class="sidebar-toggle sidebar-footer-toggle"
                    id="sidebarToggle"
                    aria-label="<%= t('common.toggleSidebar') %>"
                    title="<%= t('common.collapse') %>"
                    data-label-collapse="<%= t('common.collapse') %>"
                    data-label-expand="<%= t('common.expand') %>"
                >
                    <i class="ti ti-panel-left-close"></i>
                    <span class="sidebar-toggle-label"><%= t('common.collapse') %></span>
                </button>
                <a href="/panel/logout" class="logout-btn">
                    <i class="ti ti-logout-2"></i>
                    <span class="logout-label"><%= t('nav.logout') %></span>
                </a>
            </div>
        </nav>
        <main class="content">
            <header class="topbar">
                <div class="topbar-left">
                    <h1><%= title %></h1>
                    <span class="topbar-kicker"><%= t('dashboard.consoleKicker') %></span>
                </div>
                <div class="topbar-right">
                    <div class="topbar-utility">
                        <div class="lang-switcher lang-switcher-topbar" role="group" aria-label="<%= t('common.language') || 'Language' %>">
                            <a href="?lang=ru" class="lang-btn <%= lang === 'ru' ? 'active' : '' %>">RU</a>
                            <a href="?lang=en" class="lang-btn <%= lang === 'en' ? 'active' : '' %>">EN</a>
                        </div>
                        <div class="theme-switcher" role="group" aria-label="<%= t('common.themeSwitcher') %>">
                            <button type="button" class="theme-btn" data-theme-choice="light" aria-label="<%= t('common.lightTheme') %>" title="<%= t('common.light') %>">
                                <i class="ti ti-sun"></i>
                            </button>
                            <button type="button" class="theme-btn" data-theme-choice="dark" aria-label="<%= t('common.darkTheme') %>" title="<%= t('common.dark') %>">
                                <i class="ti ti-moon"></i>
                            </button>
                            <button type="button" class="theme-btn" data-theme-choice="system" aria-label="<%= t('common.systemTheme') %>" title="<%= t('common.system') %>">
                                <i class="ti ti-device-desktop"></i>
                            </button>
                        </div>
                    </div>
                    <span class="status-dot online"></span>
                    <span class="status-text"><%= t('common.online') %></span>
                </div>
            </header>
            <div class="main-content">
                <%- content %>
            </div>
        </main>
    </div>
    
    <script src="/js/app.js"></script>
    <script>
    // Mobile menu toggle
    function toggleMobileMenu() {
        const sidebar = document.getElementById('sidebar');
        const overlay = document.getElementById('mobileOverlay');
        sidebar.classList.toggle('active');
        overlay.classList.toggle('active');
    }
    
    // Close mobile menu on link click
    document.querySelectorAll('.nav-menu a').forEach(link => {
        link.addEventListener('click', () => {
            if (window.innerWidth <= 768) {
                toggleMobileMenu();
            }
        });
    });
    </script>
</body>
</html>
